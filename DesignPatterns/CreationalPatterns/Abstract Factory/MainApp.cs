namespace DesignPatterns.CreationalPatterns.Abstract_Factory
{

    /// <summary>
    /// The gang of four describes the abstract factory pattern as providing an interface for creating 
    /// families of related or dependent objects without specifying their concrete classes.
    /// So we already talked about what factory methods are and how they are factories responsible for
    /// creating objects, well you can think of the abstract factory pattern as a factory for creating factories,
    /// it's like the factory method pattern,
    /// but everything is encapsulated, it's a higher level of abstraction using interfaces and abstract classes.
    /// Let's take a look at this class diagram. Let'
    /// </summary>


    class MainApp
    {
        void DoWork()
        {

        }
    }
}
